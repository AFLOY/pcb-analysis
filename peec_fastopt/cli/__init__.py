"""Command-line entry points shipped with the package.

Scripts under ``examples/`` are repository material and are not installed, so
anything reachable from ``[project.scripts]`` lives here instead.
"""
